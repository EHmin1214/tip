# ── 입력 데이터 ──────────────────────────────────────────────
# 리드필드만 3.4 GB 다. GitHub 에 못 올리므로 팀 공유 드라이브로 나른다.
# 받는 방법은 README.md "데이터 준비" 참고.
inputs/

# ── 출력 ─────────────────────────────────────────────────────
# 전부 재생성 가능하다. 캐시도 마찬가지 — 지워도 다시 계산될 뿐이다.
outputs/

# ── 연구 기록 ────────────────────────────────────────────────
# 일회성 검증·진단 스크립트. 결론의 근거로 로컬에 보존하되 배포하지 않는다.
research/

# ── Sim4Life 산출물 ──────────────────────────────────────────
*.smash
*.smash_Results/
*_Input.h5
*_Output.h5

# ── 파이썬 ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-*/
build/
dist/

# ── 편집기·OS ────────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini

# Local agent/editor settings
.claude/
